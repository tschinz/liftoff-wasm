# Trunk build configuration for liftoff-wasm
# Minimal configuration for maximum compatibility across environments

[build]
# Output directory for built assets
dist = "dist"

[tools]
# Disable wasm-opt to prevent "invalid code after misc prefix" errors in CI/CD
# The Rust compiler's --release flag already provides good optimization (~278KB)
wasm-opt = false